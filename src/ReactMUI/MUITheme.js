import { createTheme } from '@mui/material/styles';

export const MUIExTheme = createTheme({
  palette: {
    primary: {
      main: '#db0d82',
    },
    secondary: {
      main: '#8b5cf6',
    },
  },
});
